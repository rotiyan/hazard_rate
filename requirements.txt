# Runtime dependencies (installed with the package)
# Development, docs and plotting tools are extras in setup.py:
#   pip install -e ".[dev]"   # tests, linting, type checking, build
#   pip install -e ".[viz]"   # matplotlib, for examples/credit_card_fraud_example.py

torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
